version: "2"
sql:
  - schema: "schema"
    queries: "queries"
    engine: "sqlite"
    gen:
      go:
        out: "sqlc"
        package: "db"
        emit_json_tags: true
        emit_interface: true
